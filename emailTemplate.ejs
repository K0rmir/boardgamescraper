<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Board Game Deals!</title>
    <style>
      .sale-price {
        font-weight: bold;
      }
      .percentage-off {
        color: red;
        font-weight: bold;
      }
    </style>
  </head>
  <body>
    <h2>Hello there!</h2>
    <p>Some board games from your wishlist are currently on sale:</p>
    <ul>
      <% for (const game of gamesOnSaleArr) { %>
      <li>
        <a href="<%= game.gameUrl %>"><%= game.gameTitle %></a>
        <span class="sale-price">£<%= game.price %></span>
        (was £<%= game.listPrice %>)
        <span class="percentage-off"><%= game.percentageSaving %>%</span>
        off
      </li>
      <% } %>
    </ul>
  </body>
</html>
